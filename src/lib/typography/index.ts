export { NBSP, normalizeTypography, type NormalizeOptions } from './normalize.ts';

/** Коротка назва для шаблонів: рядки з реєстру (course.yaml) не проходять через MDX, тож типографіку ставимо тут. */
export { normalizeTypography as typo } from './normalize.ts';
